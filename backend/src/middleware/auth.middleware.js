import jwt from "jsonwebtoken" //to import the jsonwebtoken library, which is used for creating and verifying JSON Web Tokens (JWTs). JWTs are a compact and self-contained way to securely transmit information between parties as a JSON object. This library provides methods for signing and verifying tokens, making it easier to implement authentication and authorization in web applications.
import User from "../models/user.model.js"//to import the User model, which is defined in the user.model.js file. This model represents the structure of the user data in the MongoDB database and provides methods for interacting with the user collection, such as finding, creating, and updating user documents. By importing the User model, we can perform database operations related to users in our authentication middleware and controllers.

export const protectRoute = async(req,res,next) => {//to define an asynchronous middleware function named protectRoute that takes the request (req), response (res), and next function as parameters. This middleware is responsible for protecting routes by verifying the JWT token sent in the request cookies. It checks if the token is valid, decodes it to retrieve the user ID, and then fetches the corresponding user from the database. If the token is valid and the user exists, it attaches the user information to the request object (req.user) and calls the next() function to proceed to the next middleware or route handler. If any step fails (e.g., no token, invalid token, user not found), it responds with an appropriate error message and status code.
    try{
        const token = req.cookies.jwt//to retrieve the JWT token from the request cookies. The token is expected to be stored in a cookie named "jwt". This token will be used to verify the user's authentication status and retrieve their information from the database.
        if(!token){//to check if the token is not present in the request cookies. If there is no token, it means the user is not authenticated, and the middleware responds with a 401 status code and a message indicating that the user is unauthorized due to the absence of a token. This prevents unauthenticated users from accessing protected routes and ensures that only users with a valid token can proceed.
            return res.status(401).json({message: "Unauthorized - No token provided"})//to respond with a 401 status code and a message indicating that the user is unauthorized due to the absence of a token. This prevents unauthenticated users from accessing protected routes and ensures that only users with a valid token can proceed.
        }

        const decoded = jwt.verify(token, process.env.JWT_SECRET)//to verify the JWT token using the jwt.verify() method. This method takes the token and the secret key (process.env.JWT_SECRET) as parameters and checks if the token is valid and has not been tampered with. If the token is valid, it returns the decoded payload, which typically contains the user ID and other relevant information. If the token is invalid or expired, it will throw an error, which will be caught in the catch block to handle unauthorized access.
        if(!decoded){//to check if the decoded token is falsy, which would indicate that the token is invalid. If the token is invalid, it responds with a 401 status code and a message indicating that the user is unauthorized due to an invalid token. This ensures that only users with a valid token can access protected routes and helps to prevent unauthorized access.
            return res.status(401).json({message: "Unauthorized - Invalid token"})//to respond with a 401 status code and a message indicating that the user is unauthorized due to an invalid token. This ensures that only users with a valid token can access protected routes and helps to prevent unauthorized access.
        }

        const user = await User.findById(decoded.userId).select("-password")//to find the user in the database using the user ID obtained from the decoded token. The findById method is used to retrieve the user document based on the user ID, and the select("-password") method is chained to exclude the password field from the returned user object for security reasons. This allows us to access the user's information without exposing their password, which is crucial for protecting sensitive data and maintaining security best practices.
        if(!user){//to check if the user was not found in the database. If the user does not exist, it responds with a 404 status code and a message indicating that the user is unauthorized because the user was not found. This ensures that only valid users can access protected routes and helps to prevent unauthorized access by ensuring that the token corresponds to an existing user in the database.
            return res.status(404).json({message: "Unauthorized - User not found"})//to respond with a 404 status code and a message indicating that the user is unauthorized because the user was not found. This ensures that only valid users can access protected routes and helps to prevent unauthorized access by ensuring that the token corresponds to an existing user in the database.
        }

        req.user = user//to attach the user information to the request object (req.user) so that it can be accessed in subsequent middleware functions or route handlers. This allows the application to have access to the authenticated user's details, such as their ID, full name, email, and profile picture, which can be used for authorization checks or to personalize responses based on the user's information.

        next()//to call the next() function to proceed to the next middleware or route handler. This indicates that the authentication check has passed successfully, and the user is authorized to access the protected route. By calling next(), the middleware allows the request to continue through the middleware stack and eventually reach the intended route handler that will process the request and send a response back to the client.
    }
    catch(error){
        console.log("Error in protectRoute middleware", error.message)//to catch any errors that occur during the authentication process and log the error message to the console. This helps in debugging and provides insight into what went wrong during the authentication check, such as issues with token verification or database queries.
        res.status(500).json({message:"Internal Server Error"})//to respond with a 500 status code and a message indicating that an internal server error occurred. This is a generic error response that indicates something went wrong on the server side while processing the authentication check, allowing the client to understand that the request could not be completed due to a server issue.
    }
}